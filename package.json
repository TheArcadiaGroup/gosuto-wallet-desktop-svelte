{
	"name": "gosuto-wallet",
	"version": "0.0.1",
	"private": true,
	"description": "Minimal Sveltekit + Electron starter template.",
	"main": "src/electron/index.cjs",
	"type": "module",
	"scripts": {
		"dev": "cross-env NODE_ENV=development npm run dev:all",
		"dev:all": "concurrently -n=svelte,electron -c='#ff3e00',blue \"npm run dev:svelte\" \"npm run dev:electron\"",
		"dev:svelte": "svelte-kit dev",
		"dev:electron": "electron src/electron/index.cjs",
		"build:svelte": "svelte-kit build",
		"build:all": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -mwl --config build.config.json",
		"build:linux": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -l --config build.config.json",
		"build:mac": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -m --config build.config.json",
		"build:win": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -w --config build.config.json",
		"package": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -mwl --config build.config.json --publish onTagOrDraft",
		"package:always": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -mwl --config build.config.json --publish always",
		"package:never": "rm -rf dist && rm -rf release && cross-env NODE_ENV=production && npm run build:svelte && electron-builder -mwl --config build.config.json --publish never",
		"start": "svelte-kit start",
		"prepare": "husky install"
	},
	"browserslist": [
		"Chrome 89"
	],
	"dependencies": {
		"@lapo/asn1js": "^1.2.3",
		"@tailwindcss/forms": "^0.4.0",
		"axios": "^0.26.0",
		"bip39": "^3.0.4",
		"casper-js-sdk": "^2.8.0",
		"chart.js": "^3.7.0",
		"chartjs": "^0.3.24",
		"chartjs-adapter-date-fns": "^2.0.0",
		"coingecko-api": "^1.0.10",
		"date-fns": "^2.28.0",
		"electron-context-menu": "^3.1.1",
		"electron-reloader": "^1.2.2",
		"electron-serve": "^1.1.0",
		"electron-win-state": "^1.1.22",
		"node-jose": "^2.1.0",
		"secp256k1": "^4.0.3",
		"svelte-gestures": "^1.4.1",
		"tweetnacl": "^1.0.3",
		"ws": "^8.5.0"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "1.0.0-next.22",
		"@sveltejs/kit": "1.0.0-next.202",
		"@types/coingecko-api": "^1.0.10",
		"@typescript-eslint/eslint-plugin": "^5.10.2",
		"@typescript-eslint/parser": "^5.10.2",
		"autoprefixer": "^10.4.2",
		"concurrently": "^7.0.0",
		"cross-env": "^7.0.3",
		"cssnano": "^5.0.16",
		"dotenv": "^16.0.0",
		"electron": "^17.0.0",
		"electron-builder": "23.0.3",
		"electron-connect": "^0.6.3",
		"eslint": "^8.8.0",
		"eslint-plugin-svelte": "^1.1.2",
		"husky": "^7.0.4",
		"npm-run-all": "^4.1.5",
		"postcss": "^8.4.6",
		"prettier": "^2.5.1",
		"prettier-plugin-svelte": "^2.6.0",
		"pretty-quick": "^3.1.3",
		"svelte": "^3.46.4",
		"svelte-check": "^2.4.2",
		"svelte-preprocess": "^4.10.2",
		"tailwindcss": "^3.0.18",
		"typescript": "^4.5.5",
		"vite": "^2.7.13"
	}
}
